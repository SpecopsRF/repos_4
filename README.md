 #🚀 Crypto Tracker API

REST API-сервис для получения свежей информации о курсах криптовалют (BTC, ETH, DOGE и других) на основе данных CoinGecko.  
Реализован на FastAPI, покрыт тестами, готов для контейнеризации и production-использования.

![CI/CD](https://github.com/SpecopsRF/repos_4/actions/workflows/ci-cd.yml/badge.svg)
<!-- Если добавишь DockerHub — добавь сюда badge -->

## О проекте

Вдохновлён желанием получить удобный и быcтрый API для преобразования и анализа котировок криптовалют в формате, удобном для frontend и аналитиков.  
Проект хорошо декомпозирован, легко расширяется под новые источники и типы данных.

---

## ✨ Возможности

- Получение актуальных курсов популярных криптовалют через удобное REST API
- Асинхронная бизнес-логика (Python 3.12, FastAPI)
- Кэширование запросов к CoinGecko для ускорения отклика
- Автоматические тесты для всех ключевых функций (pytest)
- Проверка безопасности, линтер/black для удобной коллективной разработки
- Готовность к деплою через Docker (Dockerfile + docker-compose)
- Автоматизированный CI/CD на базе GitHub Actions

---

## 🚀 Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/SpecopsRF/repos_4.git
   cd repos_4
Локальный запуск (через venv):


Bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
Или запуск через Docker Compose (рекомендуемый):


Bash
docker-compose up --build
После этого сервис будет доступен на http://localhost:8000/docs

🗒️ Примеры API
Доступные валюты:

GET /api/crypto/list

Возвращает список поддерживаемых криптовалют.

Получить курсы:

GET /api/crypto/price?symbols=btc,eth

Ответ:


Json
{
  "btc": {
    "usd": 38570.5
  },
  "eth": {
    "usd": 2065.3
  }
}
Health check:

GET /api/health/live

Для мониторинга доступности сервиса.

🎯 Для разработчиков
Тесты:

Bash
pytest -v
Проверка стиля и безопасности:

Bash
black app/ tests/
flake8 app/ tests/
bandit -r app/
Сборка Docker-образа вручную:

Bash
docker build -t crypto-tracker .
docker run -p 8000:8000 crypto-tracker
💡 Roadmap
 Поддержка новых валют и новых источников данных (Binance/Coinbase API)
 Добавить авторизацию и возможности сохранения портфелей
 PostgreSQL для хранения истории курсов
 Метрики для мониторинга (Prometheus)
 Автоматизация выкладки докер-образов на DockerHub
 Деплой в Kubernetes и Helm chart